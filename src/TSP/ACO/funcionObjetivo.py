import numpy as np

def funcionObjetivo(solucion,dist): # -> float:
    # Calcula la funcion objetivo de una solucion

    F_obj:float = 0
    #NumClientes:int = 
    #for i in XXXXXXXX:
    #    F_obj = 
        
    return #F_obj